# Short Clip Script (ZH/EN, 30-45 sec)

## Chinese (30-45s)
台词：
“这是 `evermemos-mcp` 的比赛证据版演示。我们用 `space_id` 做记忆隔离，用 `recall/briefing` 做跨会话恢复。正式 real-data 结果：60/60 命中，P95 1957.75ms，误归因 0，四项门禁全绿。原始 runs 数据通过 release 附件公开，可复核可审计。”

画面顺序：
1. `list_spaces` 与 `recall` 结果（8-12s）
2. `benchmark_summary.json` 关键指标（12-15s）
3. evidence release 页面 + runs.jsonl 资产（8-12s）

## English (30-45s)
Narration:
“This is the competition evidence build of `evermemos-mcp`. We isolate memory with `space_id` and restore context with `recall/briefing`. Formal real-data result: 60/60 hit rate, P95 at 1957.75ms, zero attribution error, all gates passed. Raw runs are published as a release asset for auditability.”

Shot order:
1. `list_spaces` + `recall` output (8-12s)
2. `benchmark_summary.json` headline metrics (12-15s)
3. evidence release page + `runs.jsonl` asset (8-12s)
