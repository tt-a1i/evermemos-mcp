# evermemos-mcp 用户体验优化路线图

> 最后更新：2026-03-07

## 目标

在不依赖上游 EverMemOS Cloud 先修复的前提下，优先提升 `evermemos-mcp` 在真实客户端（尤其是 Cherry Studio）里的可理解性、稳定性和可用性。

当前判断：

- 真正的核心痛点不是“检索能力不够多”，而是**用户不知道当前记忆处于什么状态**。
- 产品体验的主要落差来自：
  - Cloud 异步提取慢且状态推进不透明
  - fallback 能工作，但用户/宿主容易误解为“正式记忆已完成”
  - `space_id`、`flush`、客户端缓存这些关键操作还不够傻瓜化

Cloud 队列问题的详细证据单独记录在：`EVERMEMOS_CLOUD_QUEUE_ISSUE.zh-CN.md`

## 用户场景

### 1. 聊天助手场景（Cherry Studio / Claude 类聊天客户端）

用户目标：

- 让 AI 记住名字、偏好、称呼、沟通方式
- 新会话里立刻能“认出我是谁”

当前痛点：

- 写入后经常不是正式记忆，而是 `pending_message` / `metadata_fallback`
- 用户无法判断“是暂时记住了，还是正式处理完了”
- 客户端升级/缓存问题会让实际运行版本与预期不一致

### 2. 编程工作流场景（`coding:*`）

用户目标：

- 记住项目决策、架构取舍、项目惯例、Bug 根因
- 下一次继续同一个项目时自动恢复上下文

当前痛点：

- `space_id` 语义对普通用户仍然偏技术化
- 用户不知道该用 `recall` 还是 `fetch_history`
- `forget` 在 Cloud 下的体验不够稳定，容易伤信任

### 3. 学习/知识积累场景（`study:*`）

用户目标：

- 长期积累要点和上下文
- 新会话一键恢复主题进度

当前痛点：

- 用户更需要“时间线浏览 + briefing”，而不只是 query 搜索
- 现在产品叙事仍偏“搜索回忆”，对“复盘”支持表达不够强

### 4. 多客户端/多环境场景

用户目标：

- 在不同 MCP 客户端中都能稳定使用同一套长期记忆

当前痛点：

- 各客户端配置方式不同
- `uvx` / 缓存 / 发布版本可见性容易造成误判
- 用户难以确认自己到底连的是哪一版 MCP

## Now（现在就值得做）

### 1. 把记忆生命周期讲清楚

要解决的问题：

- 用户分不清：
  - 写入已接受
  - fallback 可用
  - 正式提取完成

建议交付：

- 统一 `remember` / `request_status` / `recall` / `briefing` 的状态语言
- 明确区分：
  - `queued`
  - `provisional`
  - `fallback`
  - `searchable`
- 在文档和客户端提示词里，把“能答出来 ≠ 正式提取完成”写透

为什么优先：

- 这是当前最大的用户认知错位来源
- 不需要等待上游修复就能显著提升体验

完成标准：

- `remember` 返回不再暗示“1-2 分钟内基本会完成”
- `recall` / `briefing` 的输出里能明确区分正式结果与 fallback 结果
- 用户文档里有一张状态说明表，解释 `queued / provisional / fallback / searchable`

### 2. 把 `request_status` 变成写后主路径

要解决的问题：

- 现在工具有了，但宿主/用户不一定会主动用起来
- 用户仍然容易把“没搜到”理解成“没记住”

建议交付：

- 把 `remember(include_status=true)` 作为推荐默认路径
- 在客户端接入指南和自动记忆 prompt 中把 `request_status` 写成标准检查动作
- 给真实客户端提供“写后检查模板”

为什么优先：

- 它是当前最小成本、最大收益的状态透明化手段

完成标准：

- 客户端接入文档和 prompt 模板都把 `include_status=true` 写成推荐做法
- 文档中有固定的“写后检查 playbook”
- 至少一个真实客户端场景（如 Cherry Studio）有可复制的写后检查流程示例

### 3. 做聊天优先客户端的稳态使用规范

要解决的问题：

- 聊天客户端最容易踩到缓存、边界、启动 briefing 这些坑

建议交付：

- 固化推荐配置：`evermemos-mcp@latest` / `@固定版本`
- 明确升级后 `uv cache clean evermemos-mcp`
- 新会话默认先 `briefing`
- 会话边界明确传 `flush=true`
- 用 Cherry Studio 作为首个完整示例，但规则本身保持通用

为什么优先：

- 这是当前最真实、最频繁的用户入口

完成标准：

- 聊天型客户端接入文档包含统一的配置、启动、升级与排障流程
- Cherry Studio 有完整示例，但文档本身不把规则绑定为 Cherry 专属
- 用户能按文档完成：升级 -> 清缓存 -> 新会话 briefing -> 正确使用 flush

## Next（接下来值得做）

### 4. 强化 `space_id` 的产品引导

建议方向：

- 提供更简单的使用模板：
  - `chat:preferences`
  - `chat:daily`
  - `coding:<repo>`
  - `study:<topic>`
- 用用户语言解释“为什么要分空间”，而不是只讲参数格式

### 5. 提升 `fetch_history` 的产品地位

建议方向：

- 把它从“附带工具”提升为“核验/复盘主路径”
- 文档里增加：
  - 删除前先看 history
  - 搜索不稳定时先看时间线

### 6. 重新定义 `forget` 的产品承诺

建议方向：

- 明确它是 best-effort
- 删前核验、删后复查
- 在文档里降低“稳定删除”的暗示

## Later（以后再做）

### 7. 等上游状态稳定后，再做“自动等到可检索”

建议方向：

- 未来可以考虑：
  - `remember_and_wait`
  - `await_searchable`
  - `memory_health`
- 但前提是上游 `queued -> success` 和 `pending_messages` 行为先稳定

### 8. 再决定是否扩大身份镜像作用域

建议方向：

- 现在先保留 `chat:*` 边界
- 等“用户画像 vs 项目偏好”语义明确后，再考虑是否扩到 `coding:*`

## 不建议现在立刻做的事

- 直接把身份镜像扩到所有 `role=user` 空间
- 过早包装“自动等待直到正式可检索”的强语义工具
- 在 Cloud 删除语义还不稳定时，把 `forget` 当主卖点推进

## 一句话总结

下一步最有价值的方向，不是“再加更多记忆能力”，而是：

**让用户清楚知道记忆现在处于什么状态、什么时候可信、该怎么稳定使用。**
