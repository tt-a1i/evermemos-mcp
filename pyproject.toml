[project]
name = "evermemos-mcp"
version = "0.4.4"
description = "MCP Server that brings long-term memory to AI coding tools via EverMemOS"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
]

[project.urls]
Homepage = "https://github.com/tt-a1i/evermemos-mcp"
Repository = "https://github.com/tt-a1i/evermemos-mcp"
Issues = "https://github.com/tt-a1i/evermemos-mcp/issues"

[project.scripts]
evermemos-mcp = "evermemos_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/evermemos_mcp"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "integration: tests that call real EverMemOS APIs (disabled by default)",
]

[tool.ruff]
line-length = 88
target-version = "py311"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "ruff>=0.12.5",
]
